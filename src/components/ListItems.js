import React from "react";

function ListItems() {
  const number = [1, 2, 3, 4, 5, 6, 7];
  return (
    <ul>
      {number.map((n) => (
        <li key={n.id}>{n}</li>
      ))}
    </ul>
  );
}

export default ListItems;

try {
   
 } catch (error) {

 } 

const bil = awa
let b 
